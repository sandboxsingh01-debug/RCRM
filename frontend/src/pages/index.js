import React from 'react';
import { Container, Typography } from '@mui/material';

export const Customers = () => (
  <Container maxWidth="lg" sx={{ mt: 4 }}>
    <Typography variant="h4">Customers Management</Typography>
  </Container>
);

export const Tickets = () => (
  <Container maxWidth="lg" sx={{ mt: 4 }}>
    <Typography variant="h4">Support Tickets</Typography>
  </Container>
);

export const Training = () => (
  <Container maxWidth="lg" sx={{ mt: 4 }}>
    <Typography variant="h4">Training Schedule</Typography>
  </Container>
);

export const Transactions = () => (
  <Container maxWidth="lg" sx={{ mt: 4 }}>
    <Typography variant="h4">Transactions & Payments</Typography>
  </Container>
);

export const Reports = () => (
  <Container maxWidth="lg" sx={{ mt: 4 }}>
    <Typography variant="h4">Reports & Analytics</Typography>
  </Container>
);

export const Settings = () => (
  <Container maxWidth="lg" sx={{ mt: 4 }}>
    <Typography variant="h4">System Settings</Typography>
  </Container>
);

export const CustomerPortal = () => (
  <Container maxWidth="lg" sx={{ mt: 4 }}>
    <Typography variant="h4">Customer Portal</Typography>
  </Container>
);
